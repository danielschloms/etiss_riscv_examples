if (VERILATOR)
    set(SRC "${CMAKE_CURRENT_SOURCE_DIR}")
    include(${PROJECT_SOURCE_DIR}/cmake/verilator.cmake)
    add_bin(basic_asm ${SRC} ${BUILD_DIR}/vector ${SRC}/basic_asm.S)
    add_bin(cpptest ${SRC} ${BUILD_DIR}/vector ${SRC}/cpptest.cpp)
    add_bin(rvv_asm_bench ${SRC} ${BUILD_DIR}/vector ${SRC}/rvv_asm_bench.S)
    add_bin(rvvbench ${SRC} ${BUILD_DIR}/vector ${SRC}/rvvbench.cpp)
    add_bin(instrs ${SRC} ${BUILD_DIR}/vector ${SRC}/instr_rvv/main.c ${SRC}/instr_rvv/bench.S)
    target_compile_definitions(instrs PRIVATE SKIP_FENCEI)
else()
    add_executable_etiss(basic_asm basic_asm.S)
    add_executable_etiss(cpptest cpptest.cpp)
    add_executable_etiss(rvv_asm_bench rvv_asm_bench.S)
    add_executable_etiss(rvvbench rvvbench.cpp)
    add_executable_etiss(instrs instr_rvv/main.c instr_rvv/bench.S)
    target_compile_definitions(instrs PRIVATE SKIP_FENCEI)
endif()
