if (VERILATOR)
    include(${PROJECT_SOURCE_DIR}/cmake/verilator.cmake)
    # add_bin(vector_add ${CMAKE_CURRENT_SOURCE_DIR} ${BUILD_DIR}/vector c)
    # add_bin(bench ${CMAKE_CURRENT_SOURCE_DIR} ${BUILD_DIR}/vector c)
    # add_bin(cpptest ${CMAKE_CURRENT_SOURCE_DIR} ${BUILD_DIR}/vector cpp)
    # add_bin(basic ${CMAKE_CURRENT_SOURCE_DIR} ${BUILD_DIR}/vector c)
    add_bin(basic_asm ${CMAKE_CURRENT_SOURCE_DIR} ${BUILD_DIR}/vector S)
    add_bin(rvvbench ${CMAKE_CURRENT_SOURCE_DIR} ${BUILD_DIR}/vector cpp)
else()
    # add_executable_etiss(vector_add vector_add.c)
    # add_executable_etiss(bench bench.c)
    # add_executable_etiss(vector_add_float vector_add_float.c)
    # add_executable_etiss(basic basic.c)
    # add_executable_etiss(cpptest cpptest.cpp)
    add_executable_etiss(basic_asm basic_asm.S)
    add_executable_etiss(rvvbench rvvbench.cpp)
endif()
